<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CREATE PIN - PINTEREST</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css" integrity="sha512-DxV+EoADOkOygM4IR9yXP8Sb2qwgidEmeqAEmDKIOfPRQZOWbXCzLC6vjbZyy0vPisbH2SyW27+ddLVCN+OMzQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="/css/style.css">
 

</head>
<body class="m-0 p-0 box-border bg-[#f0f0f0] font-sans overflow-y-auto">

   <div class="grid grid-cols-1 md:grid-cols-[80px_1fr] h-screen overflow-y-auto ">

    <!-- Sidebar -->
    <aside class="fixed md:static bottom-0 left-0 z-10 w-full md:w-20 h-16  md:h-full bg-white border-t-2 md:border-t-0 md:border-r-2 border-gray-200 flex md:flex-col flex-row justify-between items-center md:items-stretch p-2 gap-4 md:gap-20">
      <div class="flex md:flex-col flex-row gap-4 items-center justify-evenly md:h-2/3 w-full  ">
        <a href="/pinterest/home">
          <div class="w-10 h-10 overflow-hidden">
            <img src="/images/pinterestlogo.jpg" alt="Pinterest Logo" class="p-1 w-full h-full object-cover rounded" />
          </div>
        </a>
        <a href="/pinterest/home" class=  "text-black hover:text-gray-600"><i class="fa-solid fa-house text-xl" title="Home"></i></a>
        <a href="/pinterest/profile" class= "text-black hover:text-gray-600"><i class="fa-solid fa-user text-xl" title="profile"></i></a>
        <a href="/pinterest/pin/saved" class="text-black hover:text-gray-600"><i class="fa-solid fa-bookmark text-xl" title="Saved"></i> </a>
        <a href="/pinterest/pin" class=  "text-black hover:text-gray-600"><i class="fa-solid fa-circle-plus text-xl" title="Create"></i></a>
         </div>
      <div class="hidden md:flex items-center justify-center md:mb-4 ">
  <!-- Settings Icon Button -->
  <button id="settingsBtn" class="p-2 hover:bg-gray-100 rounded-full focus:outline-none">
    <i class="fas fa-cog text-xl"></i>
  </button>

  <!-- Dropdown Menu (Initially Hidden) -->
  <div id="settingsMenu" class="hidden absolute right-0 mt-2 w-40 bg-white border rounded-lg shadow-lg z-50 ">
   <a href="/pinterest/profile" class="block px-4 py-2 hover:bg-gray-100">Profile</a>
    <form action="/pinterest/logout" method="POST">
      <button type="submit" class="w-full text-left px-4 py-2 hover:bg-gray-100">Logout</button>
    </form>
</div>

      </div>
    </aside>

    <!-- Main Content -->
    <main class="grid grid-rows-[80px_auto_1fr] h-screen overflow-y-auto">

      <!-- Top Bar -->
      <section class="sticky top-0 bg-white z-10 border-b border-gray-300 p-4">
        <div class="flex items-center mb-4">
         <!-- Search bar takes all space except buttons -->
<div class="flex items-center flex-1">
  <form action="/pinterest/pin/search" method="post" class="flex w-full">
    <!-- Search icon -->
    <div class="bg-gray-200 h-10 w-10 rounded-l-xl flex items-center justify-center">
      <i class="fa-solid fa-magnifying-glass text-gray-600"></i>
    </div>

    <!-- Search input -->
    <input
      type="text"
      name="query"
      placeholder="Search Your Pins"
      class="bg-gray-200 text-gray-800 h-10 w-full rounded-r-xl px-3 text-sm md:text-base outline-none"
    />

    <!-- Optional submit button (hidden for icon-based search) -->
    <input type="submit" value="Search" class="hidden" />
  </form>
</div> </div>
      </section>

      <!-- Page Header -->
      <section class="bg-white border-b border-gray-300 p-5 pt-7 font-bold text-2xl">
        CREATE PIN
      </section>

      <!-- Form Section (scrollable) -->
      <section class="overflow-y-auto  p-4  ">
        <form action="/pinterest/pin" method="post" enctype="multipart/form-data" class="grid grid-cols-1 md:grid-cols-2 gap-6 min-h-[calc(100vh-160px)] bg-[#f0f0f0]">
          
          <!-- File Upload -->
          <!-- <div class="flex items-center justify-center bg-white rounded-2xl shadow-sm border border-gray-300 overflow-hidden min-h-[300px]">
            <label for="imageUpload" class="cursor-pointer w-full h-full flex flex-col items-center justify-center p-6 hover:bg-gray-50 transition text-center">
              <svg class="w-12 h-12 text-gray-400 mb-3" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 16v-4m0 0V8m0 4h4m-4 0H8m13 4.5V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2h6.5"></path>
              </svg>
              <p class="text-gray-500 text-sm">Click to upload or drag an image here</p>
              <input type="file" id="imageUpload" name="file" class="hidden" />
            </label>
          </div> -->
          <div class="flex flex-col items-center justify-center  rounded-2xl shadow-sm border border-gray-300 overflow-hidden min-h-[300px]">
  <label for="imageUpload" class="cursor-pointer w-full h-full flex flex-col items-center justify-center p-6 hover:bg-gray-50 transition text-center">
    <svg class="w-12 h-12 text-gray-400 mb-3" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" d="M12 16v-4m0 0V8m0 4h4m-4 0H8m13 4.5V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2h6.5"></path>
    </svg>
    <p class="text-gray-500 text-sm">Click to upload or drag an image here</p>
    <input type="file" id="imageUpload" name="file" accept="image/*" class="hidden" />
      <div class="mt-4">
    <img id="imagePreview" src="#" alt="Image Preview" class=" w-full h-full z-10 object-cover border rounded" />
  </div>
</div>
  </label>

          <!-- Text Inputs -->
          <div class="flex flex-col gap-4 bg-white p-6 rounded-2xl shadow-sm border border-gray-300">
            <input type="text" name="title" placeholder="Add your title" class="p-3 text-sm border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-red-400" />
            <textarea name="description" placeholder="Tell everyone what your Pin is about" rows="4" class="p-3 text-sm border border-gray-300 rounded-xl resize-none focus:outline-none focus:ring-2 focus:ring-red-400"></textarea>
              <label for="category" class="block text-sm font-medium text-gray-700">Category</label>
  <select name="category" id="category" required class="p-3 text-sm border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-red-400">
    <option disabled selected value="">Select a category</option>
    <option value="Art">Art</option>
    <option value="Travel">Travel</option>
    <option value="Food">Food</option>
    <option value="Technology">Technology</option>
    <option value="Fashion">Fashion</option>
  </select>
            <button type="submit" class="mt-4 mb-10 bg-red-500 text-white font-medium py-2 rounded-xl hover:bg-red-600 transition">
              Create Pin
            </button>
          </div>

        </form>
      </section>

    </main>
  </div>
  <script src="/scripts.js/home.js"></script>
  <script src="/scripts.js/form.js"></script>
</body>
</html>

